box: marvin/docker@0.0.3

build:
  steps:
    - nhuray/docker-build:
        tag: rgrecour/fileupload-webapp:${WERCKER_GIT_COMMIT:0:7}

    - nhuray/docker-push:
        image: rgrecour/fileupload-webapp:${WERCKER_GIT_COMMIT:0:7}
        email: ${DOCKER_EMAIL}
        password: ${DOCKER_PASSWORD}
        username: ${DOCKER_USERNAME}